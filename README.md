# snr_data_analyst_lendable

- Data Quality and assuptions

- Steps
- created virtual env
- read `.csv` into my .py script
- fired up my dbt project with necessary pip installs
- placed `.csv` into `/project/seeds` folder but duckdb doesn't allow id's of the dataset size as integer so must be casted to string. done using python pandas
